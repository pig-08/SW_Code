using YIS.Code.Combat;
using UnityEngine;

namespace PSB.Code.BattleCode.Players
{
    public class InventoryInputBinder : MonoBehaviour
    {
        /*[SerializeField] private PlayerInputSO input;
        [SerializeField] private ItemMainPanelUI inventoryUI;

        private void OnEnable()
        {
            input.OnInvenPressed += OnInventoryPressed;
        }

        private void OnDisable()
        {
            input.OnInvenPressed -= OnInventoryPressed;
        }

        private void OnInventoryPressed()
        {
            if (inventoryUI == null || (inventoryUI != null && inventoryUI.IsBlockingClose()))
                return;

            inventoryUI.Toggle();
        }*/
        
    }
}